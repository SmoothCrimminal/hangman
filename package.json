{
    "scripts": {
        "format": "prettier --write app",
        "start": "http-server"
    },

    "dependencies": {
        "http-server": "^13.0.0"
    },
    "devDependencies": {
        "prettier": "^2.3.2"
    }
}
